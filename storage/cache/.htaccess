# Internal cache — AI responses, computed HTML fragments, rate-limit
# counters. Never meant to be served over HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
